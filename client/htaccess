RewriteEngine On
RewriteBase /

# Если запрос идёт к существующему файлу или папке — отдаём как есть
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Иначе — перенаправляем всё на index.html (React Router сам разберётся)
RewriteRule ^ index.html [L]
